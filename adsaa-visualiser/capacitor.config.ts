import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.algovisual.pro',
    appName: 'AlgoVisual Pro',
    webDir: 'dist'
};

export default config;
